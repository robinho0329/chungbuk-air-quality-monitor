# Streamlit Cloud 배포용 (대시보드만 실행). 데이터 수집은 GitHub Actions가 담당.
# 로컬 개발은 uv sync (pyproject.toml + uv.lock)가 정본.
streamlit>=1.57
plotly>=6.0
pandas>=2.2
numpy>=2.0
sqlmodel>=0.0.22
python-dotenv>=1.1
loguru>=0.7
